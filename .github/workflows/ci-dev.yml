name: CI - DEV (feature -> dev)

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - dev

jobs:
  checks:
    name: Validate PR into dev
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Enforce branch naming convention
        run: |
          if [[ "${{ github.head_ref }}" != feature/* ]]; then
            echo "::error::PRs into 'dev' must come from a 'feature/*' branch (got '${{ github.head_ref }}')."
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate parameter.yml
        run: python -c "import yaml; yaml.safe_load(open('workspace/parameter.yml'))"

      - name: Python syntax check
        run: python -m py_compile scripts/*.py

      - name: Scan for secrets
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
